{
    "name": "case-dp6-rafael-castro",
    "version": "1.0.0",
    "description": "Techinical test for dp6",
    "main": "index.js",
    "scripts": {
        "start": "browser-sync start --server 'docs' --files 'docs'",
        "server": "browser-sync start --port 8080 --server docs --no-open",
        "cypress": "cypress run --config pageLoadTimeout=10000,baseUrl=http://localhost:8080",
        "test": "run-p -r server cypress"
    },
    "author": "Rafael Castro <rccastrorafael@gmail.com>",
    "license": "ISC",
    "devDependencies": {
        "browser-sync": "^2.27.5",
        "cypress": "^8.5.0",
        "npm-run-all": "^4.1.5"
    }
}
